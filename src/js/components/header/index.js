import React, { Component } from 'react';

class Header extends Component {
  render() {
    return (
      <div className="header">
        This will be used for a header
      </div>
    );
  }
}

export default Header;
